{
  "name": "aws-lambda-runtime-silent-fail",
  "version": "1.0.0",
  "description": "Minimal reprodution",
  "main": "index.js",
  "scripts": {
    "deploy": "sls deploy",
    "run-locally": "sls invoke local -f errorNode12"
  },
  "author": "psoares-resilient",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^1.64.0"
  }
}
